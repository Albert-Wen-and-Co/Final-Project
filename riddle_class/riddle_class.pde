void setup(){
  size(1200,800);
  background(0);
}

void draw(){
  textSize(24);
  textAlign(CENTER);
  text("What number do you get when you multiply all of the numbers on a phone's number pad?", width/2, height/4);
  fill(255,0,0);
  
}